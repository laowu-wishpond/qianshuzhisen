# 複製這個檔案為 .env（本機測試）或直接在 Vercel 專案設定 → Environment Variables 填入。
# .env 不要進 git（記得檢查 .gitignore 有排除 .env）。

# --- Supabase ---
# 專案已建好：qianshuzhisen-camping（project ref: ayehkepqoctacpbytffv）
SUPABASE_URL=https://ayehkepqoctacpbytffv.supabase.co
# service_role key 是機密金鑰，這邊不自動填，請自己去下面網址複製貼上：
# https://supabase.com/dashboard/project/ayehkepqoctacpbytffv/settings/api
SUPABASE_SERVICE_ROLE_KEY=

# --- 網站正式網址（部署到 Vercel 後的網址，結尾不要加斜線）---
SITE_URL=https://qianshuzhisen.vercel.app

# --- 綠界 ECPay ---
# 先留白 = 自動使用官方公開的「測試環境」商店代號/HashKey/HashIV（lib/ecpay.js 裡的 ECPAY_TEST_CREDENTIALS），
# 可以直接測試流程，但刷卡頁面會顯示「測試」字樣，不會真的扣款。
#
# 等客戶申請通過、拿到正式的商店代號/HashKey/HashIV 後，把下面三個填上，
# 並把 ECPAY_ENV 改成 production，就會切到正式收單網址，不用改任何程式碼。
ECPAY_MERCHANT_ID=
ECPAY_HASH_KEY=
ECPAY_HASH_IV=
ECPAY_ENV=stage
